# Development database URL (used by default during development)
DATABASE_URL=your_development_database_url

# Production database URL (used when NODE_ENV=production)
PROD_DATABASE_URL=your_production_database_url